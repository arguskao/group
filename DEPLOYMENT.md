# 快速部署指南

本文件提供快速部署問卷調查系統到 Cloudflare Pages 的步驟。

## 🚀 方法一：使用部署腳本（推薦）

### 1. 執行部署腳本

```bash
./deploy.sh
```

這個腳本會自動：
- ✅ 執行所有測試
- ✅ 建置生產版本
- ✅ 初始化 Git 儲存庫
- ✅ 提交所有變更

### 2. 推送到 Git 儲存庫

如果是第一次推送，需要設定遠端儲存庫：

```bash
# 替換成您的儲存庫 URL
git remote add origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main
```

如果已經設定過遠端儲存庫：

```bash
git push
```

### 3. 在 Cloudflare Pages 部署

1. 前往 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 點擊 **Workers & Pages** → **Create application** → **Pages**
3. 選擇 **Connect to Git**
4. 選擇您的儲存庫
5. 設定建置配置：
   - **建置指令**: `npm run build`
   - **建置輸出目錄**: `dist`
6. 點擊 **Save and Deploy**

---

## 📦 方法二：直接上傳（快速測試）

### 1. 建置專案

```bash
npm run build
```

### 2. 上傳到 Cloudflare Pages

1. 前往 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 點擊 **Workers & Pages** → **Create application** → **Pages**
3. 選擇 **Upload assets**
4. 將整個 `dist` 資料夾拖曳到上傳區域
5. 點擊 **Deploy site**

---

## 🔑 重要資訊

### 管理員密碼
下載 CSV 檔案需要管理員密碼：**3939889**

### 修改管理員密碼
如需修改密碼，請編輯 `src/constants.js` 檔案：

```javascript
export const ADMIN_PASSWORD = '您的新密碼';
```

修改後需要重新建置和部署。

---

## ✅ 部署檢查清單

- [ ] 所有測試通過 (`bun test:vitest`)
- [ ] 建置成功 (`npm run build`)
- [ ] Git 儲存庫已初始化
- [ ] 程式碼已推送到遠端儲存庫
- [ ] Cloudflare Pages 專案已建立
- [ ] 建置配置正確設定
- [ ] 部署成功並可訪問
- [ ] 測試表單提交功能
- [ ] 測試統計顯示功能
- [ ] 測試 CSV 下載功能（使用管理員密碼）

---

## 🔧 故障排除

### 建置失敗
- 確認已安裝所有依賴：`npm install` 或 `bun install`
- 檢查 Node.js 版本是否符合要求

### 部署後功能異常
- 檢查瀏覽器控制台是否有錯誤訊息
- 確認 localStorage 在您的瀏覽器中可用
- 清除瀏覽器快取後重試

### Git 推送失敗
- 確認已設定 Git 遠端儲存庫
- 檢查是否有權限推送到該儲存庫
- 確認分支名稱正確（main 或 master）

---

## 📞 需要協助？

如有任何問題，請查看：
- [README.md](./README.md) - 完整文件
- [Cloudflare Pages 文件](https://developers.cloudflare.com/pages/)
